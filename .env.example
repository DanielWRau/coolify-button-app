# Server Configuration
PORT=3000
NODE_ENV=production

# Application Password (change this!)
APP_PASSWORD=your_secure_password_here

# Session Secret (generate a random string)
SESSION_SECRET=your_random_session_secret_minimum_32_characters

# JWT Secret for authentication tokens (generate a random string)
JWT_SECRET=your_random_jwt_secret_minimum_32_characters

# Browser-Use API Configuration
BROWSER_USE_API_KEY=your_browser_use_api_key_here

# LinkedIn Credentials (for automated posting)
LINKEDIN_EMAIL=your_email@example.com
LINKEDIN_PASSWORD=your_linkedin_password

# OpenRouter AI Configuration (REQUIRED for LinkedIn post generation)
# Get your API key from: https://openrouter.ai/keys
# Used for Perplexity research + LinkedIn post generation via OpenRouter
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model Selection (for non-Perplexity tasks like article generation)
# Recommended models:
# - openai/gpt-4.1-mini (fast, cost-effective)
# - openai/gpt-4o-mini (alternative)
# - anthropic/claude-3.5-sonnet (higher quality)
OPENROUTER_MODEL=openai/gpt-4.1-mini

# Note: Perplexity research is now accessed via OpenRouter
# Model used: perplexity/sonar (via OpenRouter)
# No separate Perplexity API key needed

# Scheduled Posts Configuration (Advanced)
# NOTE: These settings provide defaults on startup but can be fully managed via Settings UI
# Changes in UI persist during runtime but are lost on restart unless ENV vars updated

# Enable/disable automatic LinkedIn posts
SCHEDULE_ENABLED=false

# Time for daily posts (24-hour format HH:MM) - LEGACY SUPPORT ONLY
# This is now managed via Settings UI with multiple posting times per day
# IMPORTANT: This is MEZ/MESZ time (Europe/Berlin)
SCHEDULE_TIME=09:00

# Timezone for scheduled posts (must match Docker timezone)
# DO NOT CHANGE unless you also update Dockerfile
SCHEDULE_TIMEZONE=Europe/Berlin

# Topics for scheduled posts (comma-separated)
# Topics are rotated sequentially (1→2→3→1)
# Fully manageable via Settings UI with add/edit/delete
SCHEDULE_TOPICS=AI in der Softwareentwicklung,Remote Work Best Practices,Leadership Lessons,Tech Trends 2025

# Advanced Features (Configured via Settings UI):
# - Multiple posting times per day with individual jitter (±30min variance)
# - Weekday selection (Mon-Sun individual toggles)
# - Sequential topic rotation with progress tracking
# - Research-enhanced posts using Perplexity API

# SMTP Email Configuration
# For sending LinkedIn posts via email (Button 2)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_smtp_password
EMAIL_FROM=noreply@example.com
EMAIL_TO=recipient@example.com

# Frontend URL (for CORS configuration)
FRONTEND_URL=https://buttons.a-g-e-n-t.de
